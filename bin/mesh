#!/usr/bin/env node
var AppBuilder = require('../lib/appbuilder');
var builder = new AppBuilder();

require('scaffolder')({
    builder: builder,
    init: [
        builder.init.bind(builder)
    ]
});
