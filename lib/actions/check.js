var events = require('events'),
    debug = require('debug')('mesh'),
    path = require('path'),
    util = require('util');
    
module.exports = function(opts, extraArgs, callback) {
    var scaffolder = this;
};