exports.install = function(mesh, instance, opts, callback) {
    var dbs = this._dbs || [];
    
    // iterate through each of the databases and listen for changes
    
};

exports.prereqs = ['couch'];